/**
 * Reserved for future ngraph / layout offload.
 * force-graph@1.x runs d3-force on the main thread; workerUrl is passed from
 * MemoryGraph for API compatibility when the engine adds worker support.
 */
self.onmessage = function () {};
